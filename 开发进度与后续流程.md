# 智能运维助手（DeepSeek Harness）—— 开发进度与后续流程

> 生成日期：2026-08-28
> 项目路径：`C:\Users\admin\WorkBuddy\2026-08-21-12-33-35\deepseek-ops-assistant\`
> 这是一份"现在做到哪了、后面怎么走"的总览，对应面试前的完整项目梳理。

---

## 一、项目是什么

一个基于 **DeepSeek Harness（dsh）** 打造的「智能运维助手」：用户用自然语言报故障/查指标，系统自动路由到**主管**或**运维诊断专家**，用真实监控数据（Go 采集器 + Prometheus）支撑结论。

```
用户问「web-01 CPU 为什么高」
        │
        ▼
┌─────────────────────────────────────────────┐
│ 主管（glm/doubao，对话 + 意图路由 + 多轮记忆） │
│   ├─ 简单查询 → 自己直接调 mcp__ops__* 工具    │
│   └─ 复杂排障 → delegate_diagnosis 拉起专家    │
└────────────┬────────────────────────────────┘
             ▼
┌─────────────────────────────────────────────┐
│ 运维诊断专家（子代理：根因分析 + 排障报告）      │
│   调 mcp__ops__query_resource_usage /        │
│      query_process / query_logs              │
└────────────┬────────────────────────────────┘
             ▼
┌─────────────────────────────────────────────┐
│ Go 监控采集器（mcp-ops.exe，自研）             │
│   stdio / streamable-http 双通道 MCP          │
│   数据源：Prometheus（CPU/内存/磁盘/网络）     │
│           Windows API（进程）文件系统（日志）  │
└─────────────────────────────────────────────┘
```

---

## 二、技术栈

| 层 | 技术 | 说明 |
|---|---|---|
| Agent 框架 | **DeepSeek Harness（dsh）0.1.1-rc.2** | Cordis 插件化，主管+专家子代理原生支持 |
| 大模型 | 火山方舟 Ark：`glm-5-2-260617` / `doubao-seed-2-0-lite-260428` | 走 OpenAI Responses 协议 |
| 领域工具 | **Go 1.26.7 + 官方 `modelcontextprotocol/go-sdk` v1.7.0** | MCP server（stdio + streamable-http） |
| 监控存储 | Prometheus 3.3.1（PromQL 查询） | 数据来自自研 exporter |
| 采集器 | `mcp-ops --exporter`（纯 Windows API，无第三方依赖） | CPU/内存/磁盘/网络/进程 |
| 部署形态 | web 模式（`dsh web`，自带 UI/会话管理） | headless 用于自动化测试 |

---

## 三、已完成内容（M1–M3）

### M1 环境与模型接入 ✅
- dsh 安装、Ark provider 配置（`/api/v3` + Responses 协议）、默认模型 `glm-5-2-260617`、真实对话跑通

### M2 主管框架 ✅
- 主管角色 = `AGENTS.md`（职责/路由/上下文纪律/转述检查），dsh 自动加载
- 验证：对话 / 文件读取 / shell 命令 / **子代理调度** 全部真实 PASS
- web 模式可访问：`dsh web --patch cordis.patch.yml --port 3080`

### M3 运维诊断专家 + 领域工具集 ✅
- **专家子代理**：`delegate_diagnosis` 工具（spawn provider，独立上下文），结果回流主管
- **3 个领域工具**（`mcp__ops__*`）：
  - `query_resource_usage`：CPU/内存/磁盘/网络，走 PromQL 查 Prometheus
  - `query_process`：进程枚举（CreateToolhelp32Snapshot）
  - `query_logs`：日志检索（纯文件读取）
- **自研 Go 采集器** `tools/mcp-ops/`（7 个 Go 文件）：
  - 三种模式：stdio（同机 MCP）/ `--http`（跨机 streamable-http）/ `--exporter`（Prometheus /metrics）
  - 纯 syscall 采集，不依赖系统命令（规避沙箱拦截）
- **意图路由重构**：主管可直接答简单查询，专家专注复杂排障（判断口诀 + 安全红线）
- **验证脚本**：`verify_promql.sh` / `verify_http.sh` / `start_prometheus.sh`

### 关键踩坑记录（面试可讲）
1. Codex Harness 与 Ark 不兼容（namespace 工具类型）→ 换 dsh（工具是扁平 function）
2. Go 进程内调 `tasklist`/`wevtutil` 被沙箱拦截 → 改纯 Windows API
3. `K32EnumProcesses` 在 psapi.dll 不存在 → 用标准 `EnumProcesses`
4. go-sdk 参数无 `omitempty` 会被判为必填 → 修复
5. 新增插件实例必须用 `- insert:`（`- id:` 只能覆盖已有条目）
6. dsh 会话锁/残留进程导致 UI 保存失败 → 清理 stale lock

---

## 四、项目文件结构

```
deepseek-ops-assistant/
├── PLAN.md                    # 实施计划（本总览的源头）
├── AGENTS.md                  # 主管角色定义（意图路由/上下文纪律）
├── cordis.patch.yml           # dsh 配置：Ark provider + 专家 + MCP 客户端
├── DSH-开发指南.md             # 从零打造智能体的完整流程指南
├── DSH-框架详解.md             # dsh 全部核心机制源码级拆解
├── DSH-插件清单.md             # web profile 约 80 个插件分类说明
├── DSH-部署与分发.md           # 打包分发四种形态（配置包/bundle/SDK/桌面壳）
├── 工具方案-Go监控采集器.md     # 领域工具方案（双通道架构/跨机部署）
├── tools/
│   ├── mcp-ops/               # Go 采集器（7 个 .go + 验证脚本）
│   │   ├── main.go            # MCP server + 三模式启动
│   │   ├── collect*.go        # 资源采集（syscall）
│   │   ├── promql.go          # Prometheus 客户端
│   │   ├── logs.go            # 日志检索
│   │   ├── exporter.go        # --http / --exporter 模式
│   │   ├── verify_http.sh     # 跨机验证
│   │   └── mcp-ops.exe        # 编译产物
│   └── prometheus/            # Prometheus 配置 + 验证脚本
└── .workbuddy/memory/         # 每日开发记录（本会话的全部过程）
```

---

## 五、后续开发流程

### 阶段 A：加固与产品化（建议优先，投入产出比最高）

| 步骤 | 内容 | 产出 |
|---|---|---|
| A1 | **Agent Preset 化**：把主管/专家从"AGENTS.md+persona"升级为 `agent.cordis.yml` 预设（角色=工具+提示封装，可版本化） | 角色可复用/可分发 |
| A2 | **技能（skill）封装**：把"CPU 排障标准流程"等写成 SKILL.md，专家按需加载（省 token、可复用） | 领域 SOP 资产 |
| A3 | **长期记忆层**：写 `memory_save/memory_query` 工具插件 + SQLite/向量库，让专家跨会话记住历史故障 | 智能体真正"越用越懂" |
| A4 | **web 形态验收**：浏览器完整体验主管→专家→工具的端到端对话 | 可演示 demo |

### 阶段 B：M4 交通研判专家（第二个专家）

| 步骤 | 内容 |
|---|---|
| B1 | 接入本地视觉模型（YOLO 检测 + 微调模型），封成 `run_vision_model` 工具（同 MCP 方式） |
| B2 | 第二个 subagent 实例：`delegate_traffic_check`（persona=交通研判专家） |
| B3 | 图片事件入口：用户发图片 → 主管路由 → 交通专家 → 结构化研判结果 |

### 阶段 C：分发与演示（秋招材料）

| 步骤 | 内容 |
|---|---|
| C1 | 按 `DSH-部署与分发.md` 的形态 A/B：打配置包或 dsh bundle，一键安装 |
| C2 | 整理面试材料：架构图、机制讲解（loop/记忆/工具流水线/沙箱）、三个踩坑案例 |
| C3 | 可选：Electron/Tauri 壳包成独立软件（形态 D） |

### 阶段 D：能力扩展（按需）

- 多目标服务器：`mcp-ops` 当监控网关，工具加 `target` 参数路由到多节点
- 日志接 Loki/ELK（替换纯文件检索）
- 告警联动：Prometheus alert 接入 dsh，主动推送
- 自定义 Cordis 插件（真正"自己写的框架扩展"）替代部分 MCP 工具

---

## 六、面试讲法（30 秒版）

> 我用 DeepSeek Harness 搭了一个智能运维助手。核心是"主管+专家"两层：主管负责意图路由，能直接调监控工具答简单查询；复杂故障会委派运维诊断专家做根因分析。数据是真实的——我用 Go 写了监控采集器（纯 Windows API），通过 MCP 接进 agent，指标走 Prometheus/PromQL。整个项目我做了三层防护：沙箱防止越权操作、提示词约束数据来源和危险操作、主管转述前做证据检查。过程中踩了不少坑（比如换框架、沙箱拦截系统命令），也都解决了。

---

*更多细节见 `DSH-框架详解.md`（机制）、`DSH-开发指南.md`（流程）、`工具方案-Go监控采集器.md`（工具架构）。*
